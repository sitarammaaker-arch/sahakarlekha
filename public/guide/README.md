# User Guide screenshots

Drop real app screenshots here with the EXACT filenames below and they appear
automatically on the `/guide` page (the `<Screenshot>` component loads
`/guide/<file>` and hides gracefully until the file exists — no broken images).

Recommended: PNG, ~1280px wide, captured at desktop width on a society with a
little sample data.

| File | Screen to capture |
|------|-------------------|
| `login.png` | Login page (email + password) |
| `dashboard.png` | Dashboard (Cash / Bank / Members / Profit cards) |
| `inventory.png` | Inventory → add item (Sales A/c + Purchase A/c fields) |
| `purchase.png` | Purchases → new purchase entry |
| `stock-adjustment.png` | Inventory → Stock Adjustment dialog |
| `trial-balance.png` | Trial Balance report |
| `balance-sheet.png` | Balance Sheet report |
